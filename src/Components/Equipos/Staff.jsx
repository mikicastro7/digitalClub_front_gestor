/* eslint-disable react/prop-types */
import React from "react";

const Staff = ({ staff }) => {
  console.log(staff);

  return (
    <section>
      <h4>Staff</h4>
    </section>
  );
};

export default Staff;
